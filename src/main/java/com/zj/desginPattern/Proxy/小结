几种常见的代理模式:

        1）防火墙代理：
                内网通过代理穿过防火墙，实现对公网的访问

        2）缓存代理：
                 比如：当请求图片文件等资源时，先到缓存代理处取，
                        如果资源请求到则OK,如果资源请求不到，再到公网取

        3）远程代理：
                 远程对象的本地代表，通过他可以吧远程对象当本地对象调用。
                 远程代理通过网络和真正的远程对象